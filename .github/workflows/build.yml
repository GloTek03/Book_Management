name: Build commonservice and bookservice

on:
  push:
    branches: [ main, master, dev-deploy-test ]
  pull_request:
    branches: [ main, master, dev-deploy-test ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build commonservice (install to local repo)
        working-directory: commonservice
        run: mvn clean install -DskipTests=true

      - name: Build bookservice
        working-directory: bookservice
        run: mvn clean install -DskipTests=true
